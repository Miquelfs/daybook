# secrets & config — never commit real secrets
.env
.env.local
.env.*.local
# .env.example is committed intentionally (template, no real values)

# databases
*.db
*.db-journal
*.db-wal
*.db-shm

# python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# data landing zones (local only, never committed)
data/raw/
data/backups/

# node / next.js
node_modules/
.next/
.vercel/

# OS
.DS_Store
Thumbs.db
